server:
  port: 8090
  
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/springboot
    username: root
    password: root
    driver-class-name: com.mysql.jdbc.Driver

mybatis:
  mapper-locations:
  - classpath:mapper/*.xml
  - classpath:mapper/expand/*.xml
  type-aliases-package: com.example.demo.pojo

logging:
  level: 
    com.example.demo.mapper : debug

    